import React from "react";
import { Link } from "react-router-dom";
import { Sheet, Card, Grid, Typography, Button } from "@mui/joy";

const SkillSheetView = ({ setStatus }) => {
  return (
    <Grid>
      <Card variant="outlined">
        <Typography level="h2">Skill Sheet View</Typography>
        <Typography level="h4">Under Construction</Typography>

        {/* insert current sheet component here */}
        {/* insert completed sheets component here */}
      </Card>
    </Grid>
  );
};

export default SkillSheetView;
