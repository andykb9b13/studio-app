import React from "react";
import { Sheet, Typography } from "@mui/joy";

const SelectPieceContainer = ({ addPieceFunc }) => {
  return (
    <Sheet>
      <Typography>Select Sheet Container</Typography>
    </Sheet>
  );
};

export default SelectPieceContainer;
